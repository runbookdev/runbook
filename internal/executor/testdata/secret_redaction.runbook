---
name: Secret Redaction Test
version: 1.0.0
description: Used to verify that secret variable values are redacted in audit logs and dry-run output.
---

```step name="use-password"
echo "connecting with {{db_password}}"
```

```step name="check-env"
printf '%s\n' "$RUNBOOK_DB_PASSWORD"
```
